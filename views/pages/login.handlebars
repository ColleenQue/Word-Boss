<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/css/style.css">
    </head>
   <article class="wholeA">
        <form action="/account/login" method="POST" class="user-log-in">
        <div id="login-form" class="myform">
            <h1>Log In</h1>
            <p>Please fill in this form to log into your account.</p>
            <hr>
            {{#if dbErr}}
                <p id="server-error">{{error}}</p>
            {{/if}}
            {{#if usernameErr}}
                <p>{{error}}</p>
            {{/if}}
            <p class = "username-err" ></p>
            <label for="username" class = "username-label">Username</label>
            <input id="username" type="text" class = "username-input" placeholder="Enter username (must have at least 2 characters)" value="{{username}}" name="username" required minlength="2">
            
            {{#if pswErr}}
                <p>{{error}}</p>
            {{/if}}
            <p class = "psw-err" ></p>
            <label for="psw" class = "psw-label">Password</label>
            <input id = "psw" type="password" class="psw-input" placeholder="Enter Password (must have at least 8 characters)" value = "{{password}}" name="psw" required minlength="8">
          
            {{!-- <div> --}}
            <button type="submit" >Log In</button>
            {{!-- </div> --}}
        </div>
        </form>
    </article>

</html>   